package model;

public class AuthData {

    /*
    Okay so this one is a identifier which auth tokens specific user
    need authtoken as string (randomized unique  string generated via something prob in thedocs
    we then just need user name


auth token public static String generateToken() {
    return UUID.randomUUID().toString();
}
     */

}





